# yaml-language-server: $schema=https://raw.githubusercontent.com/microsoft/AgentSchema/refs/heads/main/schemas/v1.0/ContainerAgent.yaml

kind: hosted
name: verbalreality
description: >
  An AI agent that builds an interactive 3D scene by generating Babylon.js
  JavaScript from natural language. The web client runs the returned code in a
  live WebGL canvas; the scene is cumulative across turns.
metadata:
  authors:
    - Microsoft
  tags:
    - Azure AI AgentServer
    - Microsoft Agent Framework
    - Babylon.js
    - 3D Scene Creation
protocols:
  - protocol: responses
    version: 1.0.0
environment_variables:
  - name: PROJECT_ENDPOINT
    value: "{{PROJECT_ENDPOINT}}"
  - name: MODEL_DEPLOYMENT_NAME
    value: "{{MODEL_DEPLOYMENT_NAME}}"
  # The validation tool calls a separate Node NullEngine service (see ./validator)
  # that only exists in local dev — it is NOT part of this container. Hosted deploys
  # therefore run with validation disabled.
  - name: ENABLE_VALIDATION
    value: "false"
dockerfile_path: Dockerfile
resources:
  cpu: '1.0'
  memory: 2.0Gi
